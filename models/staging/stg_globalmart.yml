version: 2

models:
  - name: stg_orders
    description: one unique order per row
    columns: 
      - name: orderid
        description: the praimary key for stg_orders
      - name: shipmode
        description: '{{doc("shipmode")}}'    